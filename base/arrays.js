// Array de apenas um tipo
const list = ["51980276791", "51998913436"];

// Printar lista
console.log(list);

// Printar elemento específico da lista
console.log(list[1]);

// Percorre toda a lista e printa cada valor
for(x=0; x<list.length; x++){
    console.log(list[x]);
}

// Array de vários tipos
const array = ["João", 21, 1.75];
console.log(array);